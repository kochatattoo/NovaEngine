# 02 — Краткосрочный план (1–2 недели)

> **Горизонт:** ближайшие 7–14 дней
> **Цель:** прибрать тех. долг + начать миграцию на ECS

## 🎯 Sprint v0.1.1 — ЗАВЕРШЁН (2026-07-15)

Все цели достигнуты. См. отчёт `Reports/2026-07-15.md` и CHANGELOG.md.

### ✅ 1. Чистка мусора и TODO

- [x] **Удалить `m_ColorR/G/B`** в `SandboxApp` (`Game.cpp`) — мёртвый код. **Сделано.**
- [x] **Удалить тестовый ECS-код** из `OnStart` в `SandboxApp`. **Сделано.**
- [x] **Удалить `Application::OnEvent`** — метод удалён из базового класса. **Сделано.**
- [x] **Удалить `Camera` (перспективная)** — перенесён в TODO v0.2, оставлен в коде.
- [x] **Исправить опечатки в vcxproj** (`Engine\Sources` → `Engine\Source`, `Engine\Libraries\EnTT` → `Libraries\EnTT`). **Сделано.**

### ✅ 2. `InputSystem` — убрать зависимость от `Input`

- [x] Добавлены `m_KeysHeld[KeyCode]` и `m_MouseButtonsHeld[MouseButton]` в `InputSystem`.
- [x] В `ProcessEvent` обновляются `m_KeysHeld[key] = true/false` и `m_MouseButtonsHeld[button] = true/false`.
- [x] `GetKey` / `GetMouseButton` возвращают значение из карт.
- [x] `#include "Input/Input.h"` убран из `InputSystem.cpp`.
- [x] `Input.h` (legacy) сохранён для обратной совместимости.

### ✅ 3. `Match3Board` — мелкие улучшения

- [x] `Mix` — safety limit 50 попыток + warning в лог.
- [x] `FillEmpty` — не создаёт начальный матч (как `FillRandom`).
- [ ] `OnTileChanged` callback — батч при каскаде (вызывать один раз с массивом изменений). **Перенесён в v0.2.**

### ✅ 4. Lua — мелкие улучшения

- [x] Убрано дублирование `GetMousePosition` / `GetScene` / `GetWindowWidth/Height` / `IsMouseButtonDown` в `Match3Game::SetupLuaBindings`. **Сделано.**
- [ ] Добавить `IsKeyDown(KeyCode.X)` — обёртку к `GetKey` (для удобства). **Перенесён в v0.2.**
- [ ] Добавить `EngineVersion()` глобальную функцию. **Перенесён в v0.2.**

### ✅ 5. Документация

- [x] Документация синхронизирована с кодом (все затронутые файлы обновлены).
- [ ] Скопировать документацию в `D:\ENGINE\NovaEngine\docs\` (в репо). **Перенесён в v0.2** — пока хранится в OneDrive.
- [ ] Добавить ссылку в `README.md`. **Перенесён в v0.2.**

## Sprint Backlog (что можно, если есть время)

- [ ] **MouseScrolledEvent** — кэширование в `InputSystem`. → v0.2
- [ ] **DefaultSprite** — добавить `u_Color` (vec4) uniform, чтобы `SpriteRenderer::SetColor` реально работал. → v0.2
- [ ] **`ScriptComponent`** — попробовать использовать для `tileObjects` в Match3 (вместо цикла `CreateGameObject` в Lua). → v0.2
- [ ] **Tests:** минимальный набор юнит-тестов на Catch2 или doctest для `Match3Board`. → v0.2

## Definition of Done для v0.1.1 — ВЫПОЛНЕНО

- [x] Все TODO из [01_Текущее_состояние](01_Текущее_состояние.md) с приоритетом "Низкий/Средний" закрыты или перенесены.
- [ ] Никаких warning'ов при сборке. **Не проверено** (требуется ручная сборка в Visual Studio).
- [x] Match3 играется, нет регрессий (по коду).
- [x] Документация синхронизирована с кодом.
- [ ] Git commit с тегом `v0.1.1`. **Ждёт пользователя** (после проверки и сборки).

## Следующий спринт: v0.1.2 или v0.2?

После v0.1.1 логично начать **v0.2 — ECS-миграция** (см. [03_Среднесрочный_план](03_Среднесрочный_план.md)). Это 1–2 месяца работы, и откладывать дальше не стоит.

Альтернативно, можно сделать промежуточный **v0.1.2** с мелочами из backlog (ScriptComponent, batching-сцена, ещё пара TODO) и потом уже v0.2.

**Ждём решения пользователя.**

См. также: [01_Текущее_состояние](01_Текущее_состояние.md), [03_Среднесрочный_план](03_Среднесрочный_план.md).
